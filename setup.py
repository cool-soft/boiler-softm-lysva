from setuptools import setup, find_packages

setup(
    name='boiler_softm_lysva',
    version='0.0.1',
    description='',
    url='',
    license='',
    author='Georgij',
    author_email='',
    zip_safe=False,
    packages=find_packages(
        where='src'
    ),
    package_dir={'': 'src'},
)
